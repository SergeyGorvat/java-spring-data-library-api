spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/book_library_api
    username: postgres
    password: gorvat
    driver-class-name: org.postgresql.Driver
  data:
    jdbc:
      dialect: postgresql